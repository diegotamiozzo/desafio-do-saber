# Required for Groq question generation (model openai/gpt-oss-20b)
GROQ_API_KEY=

# Server-side credentials for the mediator login. Replace both values before running.
ADMIN_USER=
ADMIN_PASS=

# Used to sign the HttpOnly mediator session cookie. Use a long random value.
AUTH_SESSION_SECRET=
